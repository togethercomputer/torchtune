# Config for multi-device full finetuning in full_finetune_distributed.py
# using a Llama3.2 1B Instruct model
#
# This config assumes that you've run the following command before launching
# this run:
#   tune download meta-llama/Llama-3.2-1B-Instruct --output-dir /tmp/Llama-3.2-1B-Instruct --ignore-patterns "original/consolidated.00.pth"
#
# To launch on 4 devices, run the following command from root:
#   tune run --nproc_per_node 4 full_finetune_distributed --config llama3_2/1B_full
#
# You can add specific overrides through the command line. For example
# to override the checkpointer directory while launching training
# you can run:
#   tune run --nproc_per_node 4 full_finetune_distributed --config llama3_2/1B_full checkpointer.checkpoint_dir=<YOUR_CHECKPOINT_DIR>
#
# This config works best when the model is being fine-tuned on 2+ GPUs.
# Single device full finetuning requires more memory optimizations. It's
# best to use 1B_full_single_device.yaml for those cases


output_dir: /data/imodoranu/results/torchtune/Llama3.2-1B-Instruct/full_distributed/${optimizer.name}/lr=${optimizer.${optimizer.name}.lr}

seed: 42
shuffle: True
batch_size: 4

# Training
epochs: 1
max_steps_per_epoch: null
gradient_accumulation_steps: 8  # Use to increase effective batch size

# Environment
device: cuda
dtype: bf16

# Memory management
enable_activation_checkpointing: False  # True reduces memory
enable_activation_offloading: False  # True reduces memory
clip_grad_norm: null
compile: True  # torch.compile the model + loss, True increases speed + decreases memory
optimizer_in_bwd: False  # True saves memory. Requires gradient_accumulation_steps=1

optimizer: # AdamW
    name: frugal_micro_adam
    adamw:
        _component_: torch.optim.AdamW
        lr: 2e-5
        weight_decay: 0
        betas:
            - 0.9
            - 0.999
        eps: 1e-8
        fused: True
    frugal_micro_adam:
        _component_: memory_efficient_optimizers.FrugalMicroAdamTorch
        lr: 2e-6
        weight_decay: 0
        betas:
            - 0.9
            - 0.999
        eps: 1e-8
        num_grads: 10
        density: 0.01
        normalization: none
    custom_ema_adamw:
        _component_: memory_efficient_optimizers.CustomEmaAdamW
        lr: 2e-5
        weight_decay: 0
        betas:
            - 0.9
            - 0.999
        eps: 1e-8
        ema_schedule: delayed
        ema_decay: 10
        ema_alpha: 0.001

#lr_scheduler:
#    _component_: torchtune.training.lr_schedulers.get_cosine_schedule_with_warmup
#    num_warmup_steps: 6250 # 10% of training

# Dataset and Sampler
dataset:
    _component_: torchtune.datasets.open_math_instruct_2_dataset
    packed: False  # True increases speed
    split: train_1M
    column_map:
        input: problem
        output: generated_solution

# Tokenizer
tokenizer:
    _component_: torchtune.models.llama3.llama3_tokenizer
    path: /data/shared/huggingface/Llama-3.2-1B-Instruct/original/tokenizer.model
    max_seq_len: null


#run_val_every_n_steps: null  # Change to an integer to enable validation every N steps
#dataset_val:
#    _component_: torchtune.datasets.alpaca_cleaned_dataset
#    split: train[95%:]
#batch_size_val: ${batch_size}

model:
    _component_: torchtune.models.llama3_2.llama3_2_1b

checkpointer:
    _component_: torchtune.training.FullModelHFCheckpointer
    checkpoint_dir: /data/shared/huggingface/Llama-3.2-1B-Instruct/
    checkpoint_files: [
        model.safetensors
    ]
    recipe_checkpoint: null
    output_dir: ${output_dir}
    model_type: LLAMA3_2
resume_from_checkpoint: False

loss:
    _component_: torchtune.modules.loss.LinearCrossEntropyLoss

# Logging
metric_logger:
    _component_: torchtune.training.metric_logging.WandBLogger
    project: ionut_torchtune_llama3.2-1B_full_ft_multi_gpu
    entity: ionutmodo
    group: ${optimizer.name}_batch-size=${batch_size}_split=${dataset.split}
    job_type: lr=${optimizer.${optimizer.name}.lr}
    name: seed=${seed}
    log_dir: ${output_dir}/logs
log_every_n_steps: 1
log_peak_memory_stats: True
log_level: INFO  # DEBUG, WARN, etc.

# Profiler (disabled)
profiler:
    _component_: torchtune.training.setup_torch_profiler
    enabled: False

    #Output directory of trace artifacts
    output_dir: ${output_dir}/profiling_outputs

    #`torch.profiler.ProfilerActivity` types to trace
    cpu: True
    cuda: True

    #trace options passed to `torch.profiler.profile`
    profile_memory: False
    with_stack: False
    record_shapes: True
    with_flops: False

    # `torch.profiler.schedule` options:
    # wait_steps -> wait, warmup_steps -> warmup, active_steps -> active, num_cycles -> repeat
    wait_steps: 5
    warmup_steps: 3
    active_steps: 2
    num_cycles: 1
